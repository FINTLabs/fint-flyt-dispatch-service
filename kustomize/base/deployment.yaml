apiVersion: apps/v1
kind: Deployment
metadata:
  name: fint-flyt-dispatch-service
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    spec:
      restartPolicy: Always
      volumes:
        - name: credentials
          secret:
            secretName: fint-flyt-dispatch-service-kafka-certificates
      containers:
        - image: ghcr.io/fintlabs/fint-flyt-dispatch-service:sha-0000000
          name: fint-flyt-dispatch-service
          volumeMounts:
            - name: credentials
              mountPath: /credentials
          resources:
            limits:
              memory: "2048Mi"
              cpu: "1"
            requests:
              memory: "512Mi"
              cpu: "100m"
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: fint-flyt-dispatch-service-kafka
            - configMapRef:
                name: fint-flyt-dispatch-service